package gui;import javafx.scene.control.PasswordField;import javafx.scene.control.TextField;public class IsValidInput {    private static PasswordField passwordField;    /**     * test integer     */    public static boolean isInt(TextField input, String message) {        try{            int age = Integer.parseInt(input.getText());//            System.out.println("age: " + age);            return true;        }catch(NumberFormatException e) {            System.out.println("Error: " + message + " is not a number!");            return false;        }    }    /**     * test valid input     * @param textField     * @return     */    public static boolean isValidAccount(TextField textField) {        if(textField.getText().isEmpty())            return false;        return textField.getText().matches("[a-zA-Z]+") && textField.getText().length() <= 20;    }    /**     * test valid input     * @param     * @return     */    public static boolean isValidPass(PasswordField message) {        if(message.getText().isEmpty())            return false;        return message.getText().matches("[a-zA-Z0-9]+") && message.getText().length() >= 8;    }}