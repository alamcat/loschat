import javafx.geometry.Pos;import javafx.scene.Scene;import javafx.scene.control.Button;import javafx.scene.control.Label;import javafx.scene.layout.HBox;import javafx.scene.layout.VBox;import javafx.stage.Modality;import javafx.stage.Stage;/** * this class is to show alert box of type modal * @author Yan * @version 2020-3-9 */public class Menu {    static void display(String title){        Stage window = new Stage();        window.setTitle(title);        //block input events or interactions with other windows        //until this one is taken care of        window.initModality(Modality.APPLICATION_MODAL); //make this window modal (模态窗口)        VBox vBox =new VBox(10);//------------------------------------------------        Button viewAll = new Button("View all users");        Button creGroup = new Button("Create groupChat");        Button exit = new Button("Exit");        viewAll.setMaxSize(150, 100);        creGroup.setMaxSize(150, 100);        exit.setMaxSize(150, 100);        //actions-viewAll        viewAll.setOnAction(e -> {            ViewAll.display("View all users");            window.close();        });        //actions-creGroup        creGroup.setOnAction(e -> {            CreateGroup.display("Create group chat");            window.close();        });        //actions-exit        exit.setOnAction(e -> {            window.close();            Exit.exit();        });        vBox.getChildren().addAll(viewAll, creGroup, exit);//------------------------------------------------        vBox.setAlignment(Pos.CENTER);//        window.setMinWidth(400);        Scene scene = new Scene(vBox, 200, 150);        window.setScene(scene);        window.showAndWait(); //shows this stage and waits for it to be hidden before return to the caller    }}